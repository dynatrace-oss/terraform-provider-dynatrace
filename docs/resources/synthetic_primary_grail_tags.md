---
layout: ""
page_title: "dynatrace_synthetic_primary_grail_tags Resource - terraform-provider-dynatrace"
subcategory: "Synthetic"
description: |-
  The resource `dynatrace_synthetic_primary_grail_tags` covers configuration of primary grail tags for synthetic monitors
---

# dynatrace_synthetic_primary_grail_tags (Resource)

-> This resource requires the API token scopes **Read settings** (`settings.read`) and **Write settings** (`settings.write`) or the OAuth scopes **Read settings** (`settings:objects:read`) and **Write settings** (`settings:objects:write`)

## Dynatrace Documentation

- Primary Grail Tags for Synthetic - https://docs.dynatrace.com/docs/observe/digital-experience/synthetic/primary-grail-tags-synthetic

- Settings API - https://www.dynatrace.com/support/help/dynatrace-api/environment-api/settings (schemaId: `builtin:synthetic.primary-grail-tags`)

## Export Example Usage

- `terraform-provider-dynatrace -export dynatrace_synthetic_primary_grail_tags` downloads the currently configured primary grail tags

The full documentation of the export feature is available [here](https://dt-url.net/h203qmc).

## Resource Example Usage

```terraform
resource "dynatrace_synthetic_primary_grail_tags" "example" {
  scope = dynatrace_http_monitor.monitor.id
  tags {
    tag {
      key   = "my-key-1"
      value = "my-value-1"
    }
    tag {
      key   = "my-key-2"
      value = "my-value-2"
    }
  }
}

resource "dynatrace_http_monitor" "monitor" {
  name      = "#name#"
  frequency = 1
  locations = [data.dynatrace_synthetic_location.location.id]
  anomaly_detection {
    loading_time_thresholds {
    }
    outage_handling {
      global_outage = true
      global_outage_policy {
        consecutive_runs = 1
      }
    }
  }
  no_script = true
}

data "dynatrace_synthetic_location" "location" {
  name = "Location"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `scope` (String) The scope of this setting (SYNTHETIC_TEST, HTTP_CHECK, MULTIPROTOCOL_MONITOR)

### Optional

- `tags` (Block List, Max: 1) Primary grail tags (see [below for nested schema](#nestedblock--tags))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--tags"></a>
### Nested Schema for `tags`

Required:

- `tag` (Block Set, Min: 1) (see [below for nested schema](#nestedblock--tags--tag))

<a id="nestedblock--tags--tag"></a>
### Nested Schema for `tags.tag`

Required:

- `key` (String) Primary grail tag's key
- `value` (String) Primary grail tag's value
